# Урок: Написание config_reader.c с нуля

## 📚 Цель урока

Научиться создавать систему загрузки конфигурации с поддержкой переменных окружения и приоритетами источников данных.

---

## 🎯 Концепция: Зачем это нужно?

### Проблема
- Токены и пароли нельзя хранить в коде или в файлах, которые попадают в Git
- Нужна гибкость: использовать разные конфиги для разработки и продакшена
- Безопасность: секреты должны быть в переменных окружения

### Решение
Создаем систему с **приоритетами**:
1. **Переменная окружения** (высший приоритет) - для секретов
2. **config.json** (низший приоритет) - для публичных настроек

---

## 📖 Часть 1: Базовые концепции C

### 1.1 Работа с переменными окружения

```c
#include <stdlib.h>  // для getenv()

// getenv() возвращает указатель на строку или NULL
const char* token = getenv("GITHUB_TOKEN");

if (token != NULL) {
    printf("Токен найден: %s\n", token);
} else {
    printf("Переменная GITHUB_TOKEN не установлена\n");
}
```

**Важно:**
- `getenv()` возвращает `const char*` - не освобождайте эту память!
- Если переменная не установлена, возвращается `NULL`
- Строка может быть пустой `""` - это тоже валидное значение

### 1.2 Проверка пустой строки

```c
#include <string.h>  // для strlen()

const char* str = getenv("GITHUB_TOKEN");

// ❌ НЕПРАВИЛЬНО: проверка только на NULL
if (str != NULL) {  // может быть пустая строка ""

// ✅ ПРАВИЛЬНО: проверка и на NULL, и на длину
if (str != NULL && strlen(str) > 0) {
    // строка не NULL и не пустая
}
```

### 1.3 Копирование строк (strdup)

```c
#include <string.h>  // для strdup()

const char* env_token = getenv("GITHUB_TOKEN");

// strdup() выделяет память и копирует строку
char* my_copy = strdup(env_token);

// Теперь my_copy - это ваша копия, её нужно освободить
free(my_copy);
```

**Почему нужен strdup?**
- `getenv()` возвращает указатель на внутренний буфер системы
- Если вы сохраните этот указатель, он может стать невалидным
- `strdup()` создает вашу копию, которую вы контролируете

---

## 📖 Часть 2: Структура данных для конфига

### 2.1 Определение структуры

```c
typedef struct {
    char* github_token;  // указатель на строку (выделена через malloc/strdup)
    // ... другие поля
} Config;
```

**Почему `char*`, а не `char[256]`?**
- Гибкость: токены могут быть разной длины
- Экономия памяти: выделяем ровно столько, сколько нужно
- Но нужно не забыть освободить через `free()`

### 2.2 Выделение памяти

```c
// Выделяем память для структуры
Config* config = malloc(sizeof(Config));

if (config == NULL) {
    fprintf(stderr, "Ошибка выделения памяти\n");
    return NULL;
}

// Инициализируем поля (чтобы избежать мусора)
config->github_token = NULL;
```

### 2.3 Освобождение памяти

```c
void free_config(Config* config) {
    if (config == NULL) return;
    
    // Освобождаем каждое поле, которое было выделено через malloc/strdup
    if (config->github_token != NULL) {
        free(config->github_token);
    }
    
    // Освобождаем саму структуру
    free(config);
}
```

---

## 📖 Часть 3: Функция get_github_token() - пошагово

### 3.1 Полный код функции

```c
const char* get_github_token(const char* token_from_json) {
    // ШАГ 1: Проверяем переменную окружения
    const char* env_token = getenv("GITHUB_TOKEN");
    
    if (env_token != NULL && strlen(env_token) > 0) {
        printf("[CONFIG] Токен загружен из переменной окружения GITHUB_TOKEN\n");
        return env_token;  // Возвращаем токен из окружения
    }
    
    // ШАГ 2: Если переменной окружения нет, используем из config.json
    if (token_from_json != NULL && strlen(token_from_json) > 0) {
        printf("[CONFIG] Токен загружен из config.json\n");
        return token_from_json;
    }
    
    // ШАГ 3: Если нигде нет - ошибка
    fprintf(stderr, "[ERROR] GitHub токен не найден!\n");
    return NULL;
}
```

### 3.2 Разбор по шагам

#### ШАГ 1: Проверка переменной окружения

```c
const char* env_token = getenv("GITHUB_TOKEN");
```

**Что происходит:**
- Система ищет переменную окружения `GITHUB_TOKEN`
- Если найдена → возвращает указатель на её значение
- Если не найдена → возвращает `NULL`

**Пример:**
```bash
# В shell:
export GITHUB_TOKEN="ghp_abc123"

# В C коде:
const char* token = getenv("GITHUB_TOKEN");
// token теперь указывает на "ghp_abc123"
```

#### Проверка на валидность

```c
if (env_token != NULL && strlen(env_token) > 0) {
    // Токен найден и не пустой
    return env_token;
}
```

**Почему две проверки?**
- `env_token != NULL` - переменная установлена
- `strlen(env_token) > 0` - переменная не пустая строка

**Сценарии:**
```bash
# Сценарий 1: Переменная не установлена
# getenv() вернет NULL
# Условие: NULL != NULL → false, пропускаем

# Сценарий 2: Переменная установлена, но пустая
export GITHUB_TOKEN=""
# getenv() вернет указатель на ""
# Условие: "" != NULL → true, но strlen("") = 0 → false, пропускаем

# Сценарий 3: Переменная установлена с токеном
export GITHUB_TOKEN="ghp_abc123"
# getenv() вернет указатель на "ghp_abc123"
# Условие: "ghp_abc123" != NULL → true, strlen("ghp_abc123") > 0 → true
# Возвращаем токен!
```

#### ШАГ 2: Fallback на config.json

```c
if (token_from_json != NULL && strlen(token_from_json) > 0) {
    return token_from_json;
}
```

**Когда это срабатывает?**
- Когда переменная окружения не установлена ИЛИ пустая
- И в `config.json` есть валидный токен

**Пример:**
```json
// config.json
{
    "github": {
        "token": "ghp_fallback_token"  // ← используется, если нет в окружении
    }
}
```

#### ШАГ 3: Обработка ошибки

```c
fprintf(stderr, "[ERROR] GitHub токен не найден!\n");
return NULL;
```

**Когда это происходит?**
- Нет переменной окружения `GITHUB_TOKEN`
- И в `config.json` токен пустой или отсутствует

**Важно:**
- Используем `fprintf(stderr, ...)` для ошибок (не `printf`)
- Возвращаем `NULL` как индикатор ошибки
- Вызывающий код должен проверить на `NULL`

---

## 📖 Часть 4: Функция load_config() - пошагово

### 4.1 Полный код функции

```c
Config* load_config(const char* config_path) {
    // ШАГ 1: Выделяем память для структуры
    Config* config = malloc(sizeof(Config));
    
    if (config == NULL) {
        fprintf(stderr, "[ERROR] Не удалось выделить память\n");
        return NULL;
    }
    
    // ШАГ 2: Читаем config.json (здесь упрощенно)
    // В реальности здесь будет парсинг JSON
    const char* token_from_json = "";  // Значение из config.json
    
    // ШАГ 3: Получаем токен с правильным приоритетом
    const char* final_token = get_github_token(token_from_json);
    
    if (final_token == NULL) {
        // Ошибка: токен не найден
        free(config);
        return NULL;
    }
    
    // ШАГ 4: Сохраняем токен в конфиге (создаем копию)
    config->github_token = strdup(final_token);
    
    if (config->github_token == NULL) {
        // Ошибка выделения памяти
        free(config);
        return NULL;
    }
    
    printf("[CONFIG] Конфиг загружен успешно\n");
    return config;
}
```

### 4.2 Разбор по шагам

#### ШАГ 1: Выделение памяти

```c
Config* config = malloc(sizeof(Config));
```

**Что происходит:**
- `sizeof(Config)` - размер структуры в байтах
- `malloc()` выделяет этот размер в куче (heap)
- Возвращает указатель или `NULL` при ошибке

**Проверка на ошибку:**
```c
if (config == NULL) {
    // malloc() не смог выделить память
    // Это редкая ситуация, но нужно обработать
    return NULL;
}
```

#### ШАГ 2: Чтение из config.json

```c
const char* token_from_json = "";  // Упрощенно
```

**В реальности здесь будет:**
```c
// Псевдокод для понимания
FILE* file = fopen(config_path, "r");
// Читаем JSON
// Парсим JSON (используя cJSON или другую библиотеку)
cJSON* json = cJSON_Parse(json_string);
cJSON* github = cJSON_GetObjectItemCaseSensitive(json, "github");
cJSON* token_obj = cJSON_GetObjectItemCaseSensitive(github, "token");
const char* token_from_json = cJSON_GetStringValue(token_obj);
// token_from_json может быть "" или "ghp_xxx"
```

#### ШАГ 3: Получение токена с приоритетом

```c
const char* final_token = get_github_token(token_from_json);
```

**Что происходит:**
- Функция проверяет приоритеты (env > json)
- Возвращает указатель на валидный токен или `NULL`

**Проверка на ошибку:**
```c
if (final_token == NULL) {
    // Токен не найден нигде
    free(config);  // Освобождаем память перед выходом
    return NULL;
}
```

#### ШАГ 4: Сохранение токена (копирование)

```c
config->github_token = strdup(final_token);
```

**Почему strdup, а не просто присвоение?**

```c
// ❌ НЕПРАВИЛЬНО:
config->github_token = final_token;  // Сохраняем указатель на чужую память

// Проблема: если final_token из getenv(), это указатель на системный буфер
// Он может стать невалидным, когда переменная окружения изменится

// ✅ ПРАВИЛЬНО:
config->github_token = strdup(final_token);  // Создаем свою копию

// Теперь config->github_token указывает на нашу память
// Мы контролируем её жизненный цикл
```

**Проверка на ошибку:**
```c
if (config->github_token == NULL) {
    // strdup() не смог выделить память
    free(config);
    return NULL;
}
```

---

## 📖 Часть 5: Полный пример использования

### 5.1 Пример main()

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
    // Загружаем конфиг
    Config* config = load_config("daemon/config/config.json");
    
    if (config == NULL) {
        fprintf(stderr, "Не удалось загрузить конфиг\n");
        return 1;
    }
    
    // Используем токен
    printf("Используем токен: %s\n", config->github_token);
    
    // ... делаем что-то с токеном (например, запросы к GitHub API)
    
    // ОБЯЗАТЕЛЬНО: Освобождаем память
    free(config->github_token);
    free(config);
    
    return 0;
}
```

### 5.2 Сценарии работы

#### Сценарий 1: Токен в переменной окружения

```bash
# В shell:
export GITHUB_TOKEN="ghp_from_env"
./daemon/gitflowd

# Вывод:
# [CONFIG] Токен загружен из переменной окружения GITHUB_TOKEN
# [CONFIG] Конфиг загружен успешно
# Используем токен: ghp_from_env
```

#### Сценарий 2: Токен в config.json

```bash
# В shell (переменная не установлена):
./daemon/gitflowd

# config.json содержит:
# { "github": { "token": "ghp_from_json" } }

# Вывод:
# [CONFIG] Токен загружен из config.json
# [CONFIG] Конфиг загружен успешно
# Используем токен: ghp_from_json
```

#### Сценарий 3: Токен нигде не найден

```bash
# В shell (переменная не установлена):
./daemon/gitflowd

# config.json содержит:
# { "github": { "token": "" } }

# Вывод:
# [ERROR] GitHub токен не найден!
# Не удалось загрузить конфиг
```

---

## 📖 Часть 6: Важные детали и best practices

### 6.1 Управление памятью

**Правило:** Что выделили через `malloc/strdup`, то освобождайте через `free`

```c
// Выделение
Config* config = malloc(sizeof(Config));
config->github_token = strdup("token");

// Использование
// ...

// Освобождение (в обратном порядке)
free(config->github_token);  // Сначала поля
free(config);                // Потом структуру
```

### 6.2 Проверка на NULL

**Всегда проверяйте:**
- Результат `malloc()` → может быть `NULL`
- Результат `strdup()` → может быть `NULL`
- Результат `getenv()` → может быть `NULL`
- Результат `load_config()` → может быть `NULL`

### 6.3 Константные указатели

```c
const char* get_github_token(...) {
    const char* env_token = getenv("GITHUB_TOKEN");
    return env_token;  // Возвращаем const char*
}
```

**Почему `const char*`?**
- Указывает, что строку нельзя изменять
- `getenv()` возвращает `const char*` - это системный буфер
- Защита от случайного изменения

### 6.4 Логирование

```c
printf("[CONFIG] ...");        // Информационные сообщения
fprintf(stderr, "[ERROR] ..."); // Ошибки
```

**Почему разные потоки?**
- `stdout` (printf) - для нормального вывода
- `stderr` (fprintf) - для ошибок
- Можно перенаправить отдельно: `./app 2> errors.log`

---

## 📖 Часть 7: Интеграция с реальным парсингом JSON

### 7.1 Пример с библиотекой cJSON

```c
#include "cjson/cJSON.h"

Config* load_config(const char* config_path) {
    Config* config = malloc(sizeof(Config));
    if (config == NULL) return NULL;
    
    // Читаем файл
    FILE* file = fopen(config_path, "r");
    if (file == NULL) {
        fprintf(stderr, "Не удалось открыть %s\n", config_path);
        free(config);
        return NULL;
    }
    
    // Получаем размер файла
    fseek(file, 0, SEEK_END);
    long length = ftell(file);
    fseek(file, 0, SEEK_SET);
    
    // Читаем содержимое
    char* json_string = malloc(length + 1);
    fread(json_string, 1, length, file);
    json_string[length] = '\0';
    fclose(file);
    
    // Парсим JSON
    cJSON* json = cJSON_Parse(json_string);
    free(json_string);
    
    if (json == NULL) {
        fprintf(stderr, "Ошибка парсинга JSON\n");
        free(config);
        return NULL;
    }
    
    // Извлекаем токен
    cJSON* github = cJSON_GetObjectItemCaseSensitive(json, "github");
    cJSON* token_obj = cJSON_GetObjectItemCaseSensitive(github, "token");
    const char* token_from_json = cJSON_GetStringValue(token_obj);
    
    // Получаем токен с приоритетом
    const char* final_token = get_github_token(token_from_json);
    
    if (final_token == NULL) {
        cJSON_Delete(json);
        free(config);
        return NULL;
    }
    
    // Сохраняем токен
    config->github_token = strdup(final_token);
    
    // Освобождаем JSON
    cJSON_Delete(json);
    
    return config;
}
```

---

## 🎓 Практические задания

### Задание 1: Добавьте функцию free_config()

```c
void free_config(Config* config) {
    // TODO: Реализуйте освобождение памяти
}
```

### Задание 2: Добавьте поле для poll_interval

```c
typedef struct {
    char* github_token;
    int poll_interval;  // ← новое поле
} Config;
```

И извлеките его из JSON.

### Задание 3: Обработка ошибок

Добавьте проверки на все возможные ошибки:
- Не удалось открыть файл
- Не удалось прочитать файл
- Ошибка парсинга JSON
- Отсутствие обязательных полей

---

## 📚 Резюме

### Ключевые концепции:

1. **Приоритеты источников:** env > json
2. **Управление памятью:** malloc/strdup → free
3. **Проверка на NULL:** всегда проверяйте результаты
4. **Копирование строк:** используйте strdup для сохранения
5. **Обработка ошибок:** возвращайте NULL и логируйте ошибки

### Структура кода:

```
get_github_token()  → Получает токен с приоритетом
load_config()       → Загружает весь конфиг
free_config()       → Освобождает память
```

### Поток данных:

```
.env файл
  ↓
load-env.sh (export GITHUB_TOKEN)
  ↓
getenv("GITHUB_TOKEN") в C коде
  ↓
get_github_token() проверяет приоритеты
  ↓
load_config() сохраняет в структуру
  ↓
Использование в приложении
```

---

## 🔗 Дополнительные ресурсы

- [man getenv](https://man7.org/linux/man-pages/man3/getenv.3.html) - документация по getenv()
- [man strdup](https://man7.org/linux/man-pages/man3/strdup.3.html) - документация по strdup()
- [cJSON библиотека](https://github.com/DaveGamble/cJSON) - для парсинга JSON

---

**Удачи в написании кода! 🚀**

